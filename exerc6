/ Online C compiler to run C program online
#include <stdio.h>

int main() {
        int opcao;
        do {
        printf("1 - Opcao 1\n");
        printf("0 - Sair 1\n");
        scanf("%d", &opcao);
       } while (opcao !=0);
    return 0;
}
